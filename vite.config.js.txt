// vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  // 💡 เพิ่ม base: './' เพื่อให้ asset paths ถูกต้องใน Production
  base: './', 
  plugins: [react()],
  // ... (โค้ดที่เหลือ)
})