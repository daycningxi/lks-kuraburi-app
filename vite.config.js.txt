// vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // ไม่มีโค้ด CSS/PostCSS/require ใดๆ ที่นี่แล้ว!
})