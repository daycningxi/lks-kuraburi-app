// vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  // 💡 Base Path สำคัญมากสำหรับการ Deploy Production
  base: './', 
  plugins: [react()],
  // 💡 ไม่ต้องมีส่วน css: { postcss: ... } แล้ว
})