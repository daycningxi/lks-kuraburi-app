// tailwind.config.js
/** @type {import('tailwindcss').Config} */
export default {
  // 💡 ลบส่วน content ออกไปชั่วคราว
  // content: [
  //   "./index.html",
  //   "./src/**/*.{js,ts,jsx,tsx}",
  //   "./src/App.jsx"
  // ],
  
  // 💡 ใส่ไฟล์ที่ใช้ Class ทั้งหมด (ซึ่งตอนนี้คือไฟล์ .jsx ทั้งหมด)
  // ใช้ Path ที่ครอบคลุมทุกไฟล์ (นี่คือโค้ดที่คุณนุ้งนิ้งเคยมี)
  content: [
    "./src/**/*.{js,ts,jsx,tsx}",
    "./index.html"
  ],
  
  // 💡 Bypassing Purge for final confirmation (ถ้าวิธีอื่นไม่ได้ผล)
  // แต่เนื่องจาก Tailwind v4 ใช้ content โดยตรง เราจะเน้นที่ Path เท่านั้น
  
  theme: {
    extend: {},
  },
  plugins: [],
}